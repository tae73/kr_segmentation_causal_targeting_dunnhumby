# =============================================================================
# Customer Segmentation & Causal Targeting — Dunnhumby
# 노트북 파이프라인(00~04)과 src/ 모듈 실행에 필요한 핵심 의존성.
# 권장: Python 3.12 가상환경에서 설치.  pip install -r requirements.txt
# =============================================================================

# --- Core data stack ---
pandas>=1.5
numpy>=1.23
scipy>=1.9

# --- Machine learning ---
scikit-learn>=1.2
xgboost>=1.7

# --- Causal inference ---
econml>=0.14          # CATE meta-learners, LinearDML, CausalForestDML
dowhy>=0.9            # causal graph / refutation utilities
statsmodels>=0.13     # OLS / inference helpers

# --- Optimization & parallelism ---
optuna>=3.0           # hyperparameter tuning (100 trials/model)
ray>=2.3              # parallel CATE fitting (graceful sequential fallback)

# --- Visualization ---
matplotlib>=3.6
seaborn>=0.12

# --- Utilities / runtime ---
joblib>=1.2           # model/result serialization (results/*.joblib)
jupyter               # notebook execution environment
